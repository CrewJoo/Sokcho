---
description: 프로덕션 빌드 및 검증
---

# 프로덕션 빌드

// turbo-all

1. 빌드를 실행합니다:
```bash
cd e:\Demo\007_PREP_5Dsay && npm run build
```

2. 빌드 성공 여부를 확인합니다. 에러가 발생하면 에러 메시지를 분석하고 수정합니다.

3. (선택) 프로덕션 서버를 로컬에서 실행합니다:
```bash
cd e:\Demo\007_PREP_5Dsay && npm run start
```
